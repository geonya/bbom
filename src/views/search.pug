extends base
include mixins/video

block content
    if errorMessage
        span#erroMessage=errorMessage
    div.video-grid
        each video in videos
            +video(video) 
        else
            span.empty__message Video not found.

    