extends base
include mixins/video


block header
    include partials/header
block content 
    div.video-grid
        each video in videos
            +video(video)
        else
            span.empty__message Video not found.
